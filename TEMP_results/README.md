# TEMP_results — index

**What this folder is**: dated, real-numbers holding notes for each experiment as it completes —
not the final dissertation write-up. Each file states how its numbers were generated (exact
commands/functions/inputs), the real results, and a headline finding, so nothing here needs to be
taken on trust or re-derived later. Delete/fold a file into the real results artifact once it's
been formally written up.

**This table is a living index, not a fixed plan** — more experiments, evaluations, and files will
be added as the work continues; update this table when they land rather than letting it drift.

**Related documents**: `documentation/research_questions_overview.md` (what each RQ asks and the
current approach — read that first for context), `jobs/rq123_methodology/README.md` (operational
run order/job counts).

**Plot/map ideas** below are condensed from the published "RQ1–RQ3 Results & Plot Atlas" artifact
— that's the full tiered (simple → complex) brainstorm with example encodings; this table only
gives the headline idea per row, and the GNNWR-dependent ones stay "TBD" until GNNWR's jobs
actually finish and sync (not done as of 2026-08-11).

| RQ | All available results | What matters (essential) / what this table answers | Experiment variants | Good plot/map idea |
|---|---|---|---|---|
| **RQ1 main sweep** | `TEMP_rq1_sweep_results_2026-08-11.tex`: `predictions.csv`, `training_history.csv`, pooled+per-fold R2/RMSE/MAE/bias with bootstrap CI, x/y joinable | **Within-neural-family model × set × cohort** — **done, answered**: DNN+Set3 beats every PINN variant on 4survey (non-overlapping 95% CIs), statistically tied (not beaten) on 6survey, confirmed by RMSE/MAE/per-fold stability. **`[STALE LABEL, corrected 2026-08-17]` This row's "winner" is only within the neural family** — the project's actual overall RQ1 winner is a tuned XGBoost, not DNN; see the baseline-comparison row below and `TEMP_rq1_xgb_hyperparameter_search_2026-08-16.tex` for the real headline | 3 models (`dnn_env_terrain`/`pinn_env_terrain`/`pinn_env_terrain_k`) × 3 sets (`nested_set2_top10`/`nested_set3_gated_terrain_wind_vif`/`nested_set4_gated_all_vif`) × 2 cohorts × 5 folds, seed 42 — **done**, 90/90 | Bar+error: pooled R2 by model×set, faceted by cohort |
| **RQ1 winner reseed** | `TEMP_rq1_winner_reseed_results_2026-08-11.tex`: pooled R2 per seed + across-seed mean/SD/bias | **Is DNN's edge over PINN/PINN\_k on 4survey a lucky single seed?** — **done, answered**: no. 4survey R2 varies only ±0.0035 across 5 seeds; 6survey SD 0.0078, one mild outlier seed, nothing overturns the original read. 6survey's negative bias is consistent across all 5 seeds — a real, seed-independent finding. This reseed is of the DNN-vs-PINN comparison specifically, not of the project's overall XGBoost-vs-neural winner (see baseline-comparison row) | DNN × Set3 × 2 cohorts × 5 folds × 4 new seeds (43-46; 42 already existed) — **done**, 40/40 | Bar+error: R2 across 5 seeds, one bar per cohort |
| **RQ1 baseline comparison** | `TEMP_rq1_baseline_comparison_2026-08-11.tex` (the actual head-to-head, Set3, now also includes `average_by_age`) + `TEMP_baseline_env_results_2026-08-09.tex`/`TEMP_baseline_env_new_sets_results_2026-08-10.tex` (raw baseline numbers) | **Does DNN/PINN's complexity beat simple baselines given the same information?** — **CORRECTED 2026-08-14, then SUPERSEDED 2026-08-16**: RQ1's XGBoost was first found running on unfair raw defaults vs. RQ3's own borrowed fixed config; refitting with that borrowed config gave XGBoost 0.675/0.718 (4survey/6survey), already beating DNN on both. **This was then superseded by a genuine 27-configuration, per-cohort hyperparameter search run specifically on RQ1's own target** (`TEMP_rq1_xgb_hyperparameter_search_2026-08-16.tex`), which is what the results chapter now cites: **0.674/0.722** — inside 0.001-0.004 of the borrowed-config number, so the two corrections agree XGBoost wins, they just differ slightly on the exact final figure; use the 2026-08-16 search's numbers as current. RF is unaffected (never used XGBoost's settings) and still loses to DNN/PINN on 6survey. `average_by_age` (naive Age-only lookup) added as a floor reference: worst on 4survey, beats Linear on 6survey | Linear/RF/XGBoost × 3 sets × 2 cohorts × (1 plot_level + 5 spatial_block_kfold folds) — **done**. `average_by_age` — reused existing run. **XGBoost hyperparameter sensitivity check — done**, 10/10 fits (Set3 × 2 cohorts × 5 folds), CPU-only local. **Full 27-config search — done**, 270/270 fits, see below | Grouped bar: baseline vs. DNN/PINN pooled R2, same set/cohort |
| **RQ1 physics-weight ablation** | `TEMP_rq1_physicsablation_results_2026-08-11.tex`: both tiers complete | **Does the physics loss itself help, not just the y_max/k architecture?** — **done, answered**: R2 decreases monotonically as weight increases (0→1→2), every model, every cohort, no exceptions; w=0 beats w=1 by +0.05 R2 on 4survey (outside both CIs, real) and +0.01 on 6survey. But w=0 makes PINN_k's y_max/k parameters far LESS identifiable (correlation -0.93 vs -0.45 at w=1) — a real accuracy-vs-interpretability tradeoff, not a clean win | Single-split: 3 weights (0/1/2) × 2 models × 2 cohorts — **done**, 12/12. 5-fold: PINN+PINN_k × 2 cohorts × 5 folds at w=0 — **done**, 20/20 | Loss curves by epoch, physics weight as the colour; bar+error R2 across the weight range |
| **RQ1 temporal check (current tier)** | `TEMP_rq1_temporalcheck_results_2026-08-11.tex`: `predictions.csv` per model/cohort/split-type | **Does spatial_block_kfold actually generalize better than temporal forecasting?** — **done**, 12/12, all 3 models both cohorts. Every model/cohort shows a large drop, PINN loses >3/4 of its R2 on 6survey (0.73→0.16-0.19) — sufficient to formally close the temporal scope question under the current methodology | DNN/PINN/PINN_k × Set3 × 2 cohorts × {spatial_block, temporal}, single split each — **done**, 12/12 | Dumbbell: spatial_block R2 → temporal R2, one row per model |
| **RQ1 plot_level integrity check** | `TEMP_rq1_plotlevel_check_results_2026-08-12.tex` — current tier | **Does spatial_block_kfold actually matter?** — **done, confirmed and sharpened**: DNN 0.831 (easy) vs 0.634 (spatial_block) = 0.197 gap, even bigger than the old tier; PINN/PINN_k show ~0 inflation on either cohort (±0.006) | DNN/PINN/PINN_k × Set3 × 2 cohorts × plot_level, single split — **done**, 6/6 | Dumbbell: easy-split R2 → spatial_block R2, one row per model |
| **RQ1 architecture-size sweep** | `TEMP_rq1_architecture_sweep_results_2026-08-13.tex` | **Was 128x128x128 the wrong architecture for the current env-conditioned models?** — **done, answered: no, for all 3 models**. DNN: no architecture wins on both cohorts (deeper best on 4survey, worst-ish on 6survey; small best on 6survey, worst on 4survey) — the signature of noise, val_loss ranking doesn't even track test-R2 ranking. **PINN/PINN_k extension**: checks the sharper concern "does PINN's combined data+physics loss need different capacity than DNN's" — answered no, and more clearly than DNN: spread across architectures is 5-10x smaller than DNN's, sits at/below the known training-seed noise floor. No limitation needs flagging for RQ2a's PINN numbers | DNN + Set3 × 4 architectures × 2 cohorts — **done**, 8/8. PINN + PINN_k × 4 architectures × 2 cohorts — **done**, 16/16. All single spatial_block split (coarse screen, matches 2026-08-02 convention), evaluate run locally, no cluster round-trip needed for any of it | Not needed — null result, reported as a table |
| **RQ2a residual reduction** | `TEMP_rq2a_residual_reduction_results_2026-08-11.tex`: `rq2_residual_reduction.csv` per row with x/y, overall + by-CR-quartile stats, Step 4a 5-seed section, XGBoost comparison (added 2026-08-14) | **Does environment help most where the shared curve is already worst?** — done, confirmed robust across all 18 combos + 5-seed pass. **XGBoost's own version checked (2026-08-14)**: does NOT give a clean "point for PINN" — XGBoost shows the same quartile-concentrated shrinkage pattern, comparable/larger average reduction than DNN. **`[CORRECTED 2026-08-17]` The stability comparison stated here originally ("XGBoost's fold-to-fold SD is 5-14x DNN's") compared DNN's 5-SEED reseed SD against XGBoost's 5-FOLD SD — not the same basis.** Compared on the same basis (single seed, 5-fold SD for both), XGBoost's SD is if anything slightly SMALLER than DNN's (69-85% of DNN's), no real stability gap — see the results chapter's RQ2a item 2. **`[ADDED 2026-08-17]` Permutation control** (`TEMP_rq2a_permutation_check_2026-08-17.tex`) resolves a further, separate open question: Q1's degradation happens almost identically when the environmental features are real vs. scrambled/fake, so it is NOT a cost of real environmental conditioning specifically, it is a cost of model flexibility in general. Q4's correction, by contrast, genuinely shrinks (18-27%) when the environment is permuted away, so that half of the effect IS real signal. Changes the dissertation's argument structure — "PINN shrinks the residual more cleanly" isn't supportable, and "environmental conditioning is a trade-off" now needs the Q1/Q4 asymmetry stated precisely rather than as one uniform trade-off. **`[CORRECTED 2026-08-18]` Residual-reduction Moran's I** (`TEMP_rq2a_reduction_morans_i_2026-08-16.tex`) was re-run under the corrected semivariogram-based method (see RQ2b row below) — the original "substantially spatially clustered, 0.55-0.64" finding reverses, not just shrinks: no reliable spatial clustering detected for either model, either cohort. The planned spatial "help map" for RQ2a is not supported and should not be built | Reuses RQ1's 90 sweep fits directly × CR baseline per fold — **done**, all 90 + 5-seed pass. **XGBoost env vs no-env, Set3 only, 2 cohorts × 5 folds × 2 arms — done**, 20/20 fits, local. **Permutation control, Set3 only, 2 cohorts × 5 folds, XGBoost only — done**, 10/10 fits, local | Quartile bar chart: mean reduction by CR-error quartile, colour = model |
| **RQ2b attribution** | `TEMP_rq2_attribution_results_2026-08-11.tex`: `nlme_fixed_effects.json`, `elastic_net_coefficients.csv`, EN/XGBoost checkpoints, pooled+fold R2 per set (all 4, incl. Set1), `xgboost_shap_values.csv` (all 3 env sets, recomputed) | **Which variables explain the CR-residual, and do NLME/EN/XGBoost agree?** — done: `CanopyCover`+thinning dominate every set, every method including SHAP, essentially unchanged by the fix. **CanopyCover reverse-causation concern checked**: supports reframing CanopyCover as a baseline control. **XGBoost bug FIXED IN PRODUCTION 2026-08-15** (`run_rq2_attribution.py` edited, not just a sensitivity check): **XGBoost now beats EN on every set** (e.g. Set4: EN 0.358 vs. XGB 0.395). `[CORRECTED 2026-08-17]` This row previously stated the fix as "Set4: 0.338→0.395," pairing the CORRECTED model's own CI lower bound (0.338) with its own point estimate (0.395) as if they were a before/after pair — they are not; the actual pre-fix Set4 value is not recorded in `TEMP_rq2_attribution_results_2026-08-11.tex` and should not be cited as 0.338. Set1 baseline-only: XGBoost (0.222) and EN (0.220) now essentially tied — the old apparent EN advantage there was a hyperparameter artifact. `[ADDED 2026-08-17]` **Why NLME and EN disagree on `topex`'s coefficient specifically (strong/stable in NLME, weak/sign-flipping in EN) now has a partial, theory-grounded answer**: `topex` is 74% between-compartment variance (`TEMP_rq2b_topex_variance_decomposition_2026-08-17.tex`), consistent with a spatial-confounding account (NLME's random intercept absorbs compartment-level correlation, EN has no equivalent term) — but `slope_degrees` (which stays stable) is also 63% between-compartment, so this doesn't cleanly separate the two variables on its own; read as "theoretically grounded, partially evidenced," not fully settled. **`[CORRECTED 2026-08-18]` EN/XGBoost residual Moran's I** was re-run under a corrected semivariogram-informed distance-band method (previous k=8 method tested near-immediate-neighbour similarity on this project's dense 20m grid, not real regional clustering) — magnitude drops from 0.65-0.74 to 0.03-0.15; EN and XGBoost no longer look similar (EN -29% Set1→Set4, XGBoost -63%, a genuine divergence the old method didn't show) | 4 sets (Set1 baseline + 3 VIF'd) × 5 folds, **4survey only** — **done**, 20/20 fits + 20/20 evaluates through the corrected pipeline. SHAP — **done**, 15/15, recomputed against corrected checkpoints. `topex`/`slope_degrees` variance decomposition — **done**, read-only, no new fits. **Moran's I re-run 2026-08-18** under corrected method | Coefficient forest plot: NLME/EN side by side; SHAP beeswarm, data ready |
| **RQ3 EN/XGBoost + SHAP** | `TEMP_rq3_en_xgb_results_2026-08-11.tex` (now also has a 6survey EN-coefficient + gain-importance section, added 2026-08-16) + `TEMP_rq3_outlier_diagnosis_results_2026-08-12.tex` + `TEMP_rq3_gnnwr_local_coef_rank_2026-08-16.tex` | **Best set, which variables matter, per-plot outlier explanation** — **all done, and corrected**: the same ~10 plots recur as the worst residual across nearly all 6 (set × cohort) combos (outlier-ness is a plot property, not a feature-set artifact). Cross-referenced against the disturbance-classification system — **0 of 10 carry any disturbance/measurement flag, and 0 of 10 clear the top-1% trajectory-instability cutoff either**, overturning the earlier "almost certainly disturbance/data-quality artifacts" read. Better-supported split: 5 plots show real (sub-threshold) trajectory instability; 5 show a smooth, internally consistent trajectory offset far from the official yldc benchmark — pointing to a yldc lookup mismatch, not disturbance. No basis found for excluding these plots from RQ3's numbers. `[ADDED 2026-08-16, not in this row before]` **6survey's `CanopyCover` rank was closed as a gap**: EN coefficient and XGBoost gain-importance were re-run for 6survey (previously only SHAP existed there) — EN and GNNWR both still rank `CanopyCover` #1 on 6survey, XGBoost (gain and SHAP alike) does not, in any set. This is a genuine 2-vs-1 split by MODEL, not one thin single-method reading. XGBoost was deliberately NOT retuned for 6survey (scope decision, matches RQ2b/RQ3's shared-config stance project-wide) | EN/XGBoost: 3 sets × 2 cohorts — **done**, 6/6 (6survey EN/gain-importance added 2026-08-16). SHAP: **done**, 6/6. Outlier diagnosis: **done**, all 6/6 combos + disturbance cross-reference. GNNWR local-coefficient rank (6survey) — **done** | Grouped bar: EN vs. XGBoost pooled R2; SHAP beeswarm |
| **RQ3 GNNWR** | `TEMP_rq3_gnnwr_results_2026-08-11.tex` (now includes the 6survey reseed correction and the 2026-08-18 Moran's I correction) | **Does GNNWR's spatially-varying fit beat global EN/XGBoost?** — **done, and corrected**: yes on 4survey (stable, reliable). 6survey's original "GNNWR loses" reading was wrong — the reseed shows R2 sign-flips across seeds for every set, mean ≈0, within the seed-to-seed SD — the honest read is "underpowered, no reliable direction," not "loses". **`[CORRECTED 2026-08-18]` Residual Moran's I vs. EN/XGBoost** re-run under the corrected semivariogram method (see RQ2b row) — 4survey's GNNWR reduction is proportionally larger than before (24-32% relative vs. ~2-3% under the old k=8 method). 6survey's original "GNNWR increases clustering, wrong direction" finding does not survive — most 6survey cells show no resolvable spatial structure at all under the corrected method, removing Moran's I as independent corroboration of GNNWR failing on 6survey (the R2 collapse still stands on its own evidence) | 3 sets × 2 cohorts × 5 folds — **done**, 30/30. 6survey reseed (2 more seeds × 3 sets × 5 folds) — **done**, 30/30, zero failures. **Moran's I re-run 2026-08-18** under corrected method, including a widened 10000m search for cells unresolved at 5000m | Local-coefficient map (data ready, not yet plotted); 3-panel EN/XGB/GNNWR residual comparison |
| **RQ3 category attribution** | `TEMP_rq3_category_attribution_results_2026-08-11.tex` (current, VIF-screened Set4) — supersedes `TEMP_rq3_category_attribution_results_2026-08-10.tex` (pre-VIF, kept as historical record) | **Which category of variable matters** — **done, and the rerun changed the finding**: `stand_structure` still dominates, but the earlier "every other category removal improves R2" pattern is gone — VIF screening fixed what looks like collinearity-driven noise; `climate`'s contribution roughly doubled; `soil_site` remains ~0 (pure noise) in both versions | Single spatial_block split, Set4/4survey only, current VIF-screened membership — **done** | Category-level permutation-importance bar chart |

---

## Discussion, analysis, and the results-chapter draft

`[CORRECTED 2026-08-17]` This section previously pointed to `a_draft_plan_14th_aug.md`, which is
now several revisions behind. The current, audited results/evaluation chapter draft is
`documentation/august_draft/5_Chapter_results_evaluation/c_draft_plan_15th_1149pm_audit.md`
(every claim checked directly against its `TEMP_results` source) and its figure-planning
companion `d_draft_plan_16th_855pm_plotting.md` (identical results text, richer plot inventory).
The condensed, prose-first write-up drafted from those two is
`e_draft_plan_17th.tex`. This folder (`TEMP_results/`) stays scoped to what it says at the top:
raw, dated, real-numbers holding notes per experiment, plus the method notes below — no
discussion/ranking/prose interpretation content here, so there's one place, not two, for that.

**Methodology → Results content migration table** (specific sentences flagged as living in the
wrong chapter — methodology currently states them as findings) also moved there, since it
cross-references the primer's item numbers directly.

---

## Reproducing the ad-hoc TEMP checks (2026-08-15 onward)

Several checks were run as one-off local snippets rather than saved as a dedicated script under
`models/`. In every case the actual statistic is computed by an existing, already-used project
function — only the *driving loop* (which files/sets/cohorts to feed it) was throwaway. All read
already-saved `predictions.csv`/`test_predictions.csv` files — none of them retrain a model. Each
has its own short method file (what was evaluated + pseudocode for how + pointer to the numbers),
so nothing here needs digging through chat history to reproduce:

| Method file | RQ | What it covers |
|---|---|---|
| `TEMP_rq2b_rq3_method_bootstrap_ci_2026-08-15.tex` | RQ2b + RQ3 | Cluster-bootstrap 95% CI on pooled R2 (EN/XGBoost, GNNWR) |
| `TEMP_rq2b_method_vif_2026-08-15.tex` | RQ2b | VIF on the SHAP-important attribution variables |
| `TEMP_rq2b_rq3_method_morans_i_2026-08-15.tex` | RQ2b + RQ3 | Residual Moran's I (EN/XGBoost/GNNWR) |
| `TEMP_rq3_method_boundary_ymaxfit_mechanism_2026-08-15.tex` | RQ3 | Why some outlier plots' `y_max_fit` is implausible; boundary-distance pull |
| `TEMP_rq3_method_broader_scan_sensitivity_2026-08-15.tex` | RQ3 | Tukey-fence scan for implausible `y_max_fit` beyond the known 10; exclusion sensitivity check |
| `TEMP_rq3_method_typology_crossmodel_2026-08-15.tex` | RQ3 | Cross-model (EN/XGBoost/GNNWR) outlier agreement; sub-compartment spatial clustering |
| `TEMP_rq3_method_2008_artifact_2026-08-15.tex` | RQ3 | Wind exposure, storm-year signature, 2008 survey-boundary artifact test |
| `TEMP_rq1_cohort_composition_2026-08-15.tex` | RQ1 | 4survey vs. 6survey elevation/compositional comparison, feeds RQ1 item 2's "why" |
| `TEMP_rq2a_pinn_k_identifiability_bootstrap_2026-08-16.tex` | RQ2a | Cluster-bootstrap CI on the pooled 5-fold `y_max`/`k` correlation |
| `TEMP_rq2a_reduction_morans_i_2026-08-16.tex` | RQ2a | Moran's I on the residual-reduction value itself (not raw residuals) |
| `TEMP_rq3_compartment_archetype_check_2026-08-16.tex` | RQ3 | Compartment-level classification by growth-curve deviation pattern |
| `TEMP_rq3_gnnwr_local_coef_rank_2026-08-16.tex` | RQ3 | GNNWR's per-plot local coefficients, ranked, for the 6survey `CanopyCover` check |
| `TEMP_rq2b_topex_variance_decomposition_2026-08-17.tex` | RQ2b | Between/within-compartment variance decomposition (ICC) for `topex` and `slope_degrees`, saved as `models/spatial_attribution/rq2b_topex_variance_decomposition.py` (a real reusable script, not a throwaway snippet, since ICC wasn't an existing project function) — checks the spatial-confounding precondition for why NLME/EN disagree on `topex` |

**Not covered by the pattern above (these DO retrain a model, saved under `models/`, not one-off
snippets)**:

| Script/file | RQ | What it covers |
|---|---|---|
| `models/baselines/xgb_hyperparameter_sensitivity_check.py` → `TEMP_rq1_xgb_hyperparameter_search_2026-08-16.tex` | RQ1 | 27-config × 5-fold × 2-cohort search, RQ1's own tuned XGBoost (the current headline config) |
| (ad-hoc, reused `run_reduction_check()`) → `TEMP_rq1_xgb_vs_dnn_paired_folds_2026-08-16.tex` | RQ1 | Paired per-fold XGBoost-vs-DNN comparison (win counts, the one reversed fold) |
| (ad-hoc, reused `rq1_results_table_metrics.py`) → `TEMP_rq1_results_table_metrics_2026-08-16.tex` | RQ1 | Recomputes the main RQ1 results table directly from each fold's saved `metrics.json` |
| `models/spatial_attribution/rq2a_permutation_check.py` → `TEMP_rq2a_permutation_check_2026-08-17.tex` | RQ2a | Environmental-feature permutation control (10 new XGBoost fits) — isolates whether Q1's degradation needs real environmental signal (see the RQ2a residual reduction row above) |
